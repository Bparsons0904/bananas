# Environment variables for Bananas development
# Copy this file to .env and adjust values as needed
# cp .env.example .env

# ==========================================
# PORTS
# ==========================================

# PostgreSQL Port (default: 5433 to avoid conflicts with system PostgreSQL on 5432)
# Change this if you have another service on 5433
DB_PORT=5433

# Tilt Dashboard Port
TILT_PORT=10350

# Backend Framework Ports (Go servers)
STANDARD_PORT=8081
GIN_PORT=8082
FIBER_PORT=8083
ECHO_PORT=8084
CHI_PORT=8085
GORILLA_PORT=8086

# Frontend Ports (managed by Tilt, usually don't need to change)
# FRONTEND_MAIN_PORT=5172
# FRONTEND_REACT_PORT=5173
# FRONTEND_SOLID_PORT=5174
# FRONTEND_ANGULAR_PORT=5175

# ==========================================
# DOCKER ENVIRONMENT
# ==========================================

# Environment: dev, staging, prod
DOCKER_ENV=dev

# ==========================================
# DATABASE SETTINGS
# ==========================================

# Database host (localhost for local dev, postgres for Docker)
DB_HOST=localhost

# Database credentials
DB_USER=bananas_user
DB_PASSWORD=bananas_pass
DB_NAME=bananas_dev

# SSL mode (disable for local development)
DB_SSL_MODE=disable

# ==========================================
# OPTIONAL: LOCAL OVERRIDES
# ==========================================
# Create .env.local for local-only overrides (gitignored)
# Example: Different DB_PORT, custom passwords, etc.
